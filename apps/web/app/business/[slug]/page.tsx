import {notFound} from "next/navigation";import Link from "next/link";
const API=()=>process.env.API_INTERNAL_BASE_URL||"http://localhost:8080";
async function getBusiness(slug:string){const r=await fetch(API()+"/v1/businesses/"+encodeURIComponent(slug),{next:{revalidate:300}});if(r.status===404)return null;if(!r.ok)throw new Error("business_api_error");return (await r.json()).data}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const b=await getBusiness(slug);if(!b)return {title:"Doanh nghiệp không tồn tại",robots:{index:false,follow:false}};return {title:b.display_name+" - Số điện thoại đã xác minh",description:b.description||("Thông tin, số điện thoại và chi nhánh đã xác minh của "+b.display_name),alternates:{canonical:"/business/"+b.slug}}}
export default async function BusinessPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const b=await getBusiness(slug);if(!b)notFound();return <main className="wrap businessPage"><div className="businessHero"><div><span className="verifiedMark">✓ Doanh nghiệp đã xác minh</span><h1>{b.display_name}</h1><p>{b.description||"Hồ sơ doanh nghiệp đã được xác minh trên SoDienThoai.com."}</p></div><div className="businessRating"><strong>{b.rating?Number(b.rating).toFixed(1):"—"}</strong><span>{b.review_count||0} đánh giá</span></div></div><div className="profileLayout"><div><section className="publicPanel"><h2>Số điện thoại chính thức</h2>{b.phones?.length?b.phones.map((p:string)=><Link className="sourceRow" key={p} href={"/phone/"+encodeURIComponent(p)}><div><b>{p}</b><span>Tra cứu uy tín và báo cáo cộng đồng</span></div><span>→</span></Link>):<div className="emptyPublic">Chưa công khai số điện thoại.</div>}</section><section className="publicPanel"><h2>Chi nhánh</h2>{b.branches?.length?b.branches.map((x:any)=><div className="businessBranch" key={x.id}><b>{x.name}{x.is_primary?" · Chi nhánh chính":""}</b><span>{x.address||x.region_code||"Chưa cập nhật địa chỉ"}</span></div>):<div className="emptyPublic">Chưa có thông tin chi nhánh.</div>}</section></div><aside><section className="publicPanel stickyPanel"><h3>Thông tin xác minh</h3><p>Hồ sơ này chỉ được công khai sau khi quy trình xác minh doanh nghiệp được duyệt.</p>{b.website_url&&<a className="primaryAction" href={b.website_url} rel="nofollow noopener" target="_blank">Website doanh nghiệp</a>}<hr/><small>Cập nhật: {new Date(b.updated_at).toLocaleDateString("vi-VN")}</small></section></aside></div></main>}
