import {redirect} from "next/navigation";import {isAdmin} from "../../../lib/admin-session";import AdminNav from "../_components/nav";import {adminAPI} from "../_lib/api";
export default async function Page(){if(!(await isAdmin()))redirect("/admin/login");const d=await adminAPI("/v1/admin/operations");const cards=[["Footprint jobs",d?.footprint_jobs],["SEO cần review",d?.seo_review],["Thông báo chưa đọc",d?.unread_notifications]];return <main className="adminShell"><AdminNav/><section className="adminMain"><header className="adminTop"><div><h1>Jobs & Queue</h1><p>Theo dõi hàng đợi và tác vụ vận hành.</p></div></header><div className="adminCards">{cards.map(([x,v])=><article key={String(x)}><small>{x}</small><strong>{v??"—"}</strong><span>Dữ liệu trực tiếp</span></article>)}</div></section></main>}
