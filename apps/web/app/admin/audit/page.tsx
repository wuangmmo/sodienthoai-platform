import {redirect} from "next/navigation";import {isAdmin} from "../../../lib/admin-session";import AdminNav from "../_components/nav";import {adminAPI} from "../_lib/api";
export default async function Page(){if(!(await isAdmin()))redirect("/admin/login");const data=await adminAPI("/v1/admin/audit?limit=100");return <main className="adminShell"><AdminNav/><section className="adminMain"><header className="adminTop"><div><h1>Audit Log</h1><p>Lịch sử hành động quản trị và thay đổi dữ liệu.</p></div></header><article className="adminPanel">{data?.data?.map((x:any)=><div className="adminRecord" key={x.id}><div><b>{x.action}</b><p>{x.entity_type} · {x.entity_id}</p></div><small>{String(x.created_at||"")}</small></div>)}{!data?.data?.length&&<div className="emptyAdmin">Chưa có audit log.</div>}</article></section></main>}
