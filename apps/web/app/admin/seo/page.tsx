import {redirect} from "next/navigation";import {isAdmin} from "../../../lib/admin-session";import AdminNav from "../_components/nav";import {adminAPI} from "../_lib/api";
export default async function Page(){if(!(await isAdmin()))redirect("/admin/login");const base=process.env.API_INTERNAL_BASE_URL||"http://localhost:8080";const [ops,count]=await Promise.all([adminAPI("/v1/admin/operations"),fetch(base+"/v1/seo/sitemap/count",{cache:"no-store"}).then(r=>r.ok?r.json():null).catch(()=>null)]);const cards=[["URL sitemap",count?.count],["Kích thước sitemap",count?.page_size],["SEO cần review",ops?.seo_review]];return <main className="adminShell"><AdminNav/><section className="adminMain"><header className="adminTop"><div><h1>SEO & Index</h1><p>Theo dõi indexability và quy mô sitemap.</p></div></header><div className="adminCards">{cards.map(([x,v])=><article key={String(x)}><small>{x}</small><strong>{v??"—"}</strong><span>Dữ liệu trực tiếp</span></article>)}</div></section></main>}
