import {redirect} from "next/navigation";import {isAdmin} from "../../../lib/admin-session";import AdminNav from "../_components/nav";import {adminAPI} from "../_lib/api";export default async function Page(){if(!(await isAdmin()))redirect("/admin/login");const d=await adminAPI("/v1/admin/users?limit=100");return <main className="adminShell"><AdminNav/><section className="adminMain"><header className="adminTop"><div><h1>Users</h1><p>Tài khoản/session cộng đồng đã được hệ thống ghi nhận.</p></div></header><article className="adminPanel">{d?.data?.map((x:any)=><div className="adminRecord" key={x.id}><div><b>{x.subject}</b><p>{x.created_at}</p></div></div>)}{!d?.data?.length&&<div className="emptyAdmin">Chưa có user.</div>}</article></section></main>}