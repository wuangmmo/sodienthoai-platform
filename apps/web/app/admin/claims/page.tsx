import {redirect} from "next/navigation";import {isAdmin} from "../../../lib/admin-session";import AdminNav from "../_components/nav";import {adminAPI} from "../_lib/api";
export default async function Page(){if(!(await isAdmin()))redirect("/admin/login");const data=await adminAPI("/v1/admin/claims?limit=100");return <main className="adminShell"><AdminNav/><section className="adminMain"><header className="adminTop"><div><h1>Claims</h1><p>Yêu cầu xác nhận quyền sở hữu và danh tính số điện thoại.</p></div></header><article className="adminPanel">{data?.data?.map((x:any)=><div className="adminRecord" key={x.id}><div><b>{x.e164||x.phone_number_id}</b><p>{x.name||x.kind||"Yêu cầu xác minh"}</p></div><span className="adminBadge">{x.status}</span></div>)}{!data?.data?.length&&<div className="emptyAdmin">Không có claim.</div>}</article></section></main>}
