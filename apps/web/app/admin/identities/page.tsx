import {redirect} from "next/navigation";import {isAdmin} from "../../../lib/admin-session";import AdminNav from "../_components/nav";import {adminAPI} from "../_lib/api";export default async function Page(){if(!(await isAdmin()))redirect("/admin/login");const d=await adminAPI("/v1/admin/identities?limit=100");return <main className="adminShell"><AdminNav/><section className="adminMain"><header className="adminTop"><div><h1>Danh tính & xác minh</h1><p>Theo dõi danh tính công khai, độ tin cậy và trạng thái xác minh của số điện thoại.</p></div></header><article className="adminPanel">{d?.data?.map((x:any)=><div className="adminRecord" key={x.id}><div><b>{x.e164} · {x.display_name}</b><p>{x.kind} · confidence {Math.round((x.confidence_score||0)*100)}%</p></div><span className="adminBadge">{x.verification_status}</span></div>)}{!d?.data?.length&&<div className="emptyAdmin">Chưa có danh tính.</div>}</article></section></main>}