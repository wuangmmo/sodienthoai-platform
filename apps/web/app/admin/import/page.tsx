import {redirect} from "next/navigation";import {isAdmin} from "../../../lib/admin-session";import AdminNav from "../_components/nav";import {adminAPI} from "../_lib/api";import ImportForm from "./import-form";export default async function Page(){if(!(await isAdmin()))redirect("/admin/login");const d=await adminAPI("/v1/admin/import/batches");return <main className="adminShell"><AdminNav/><section className="adminMain"><header className="adminTop"><div><h1>Import dữ liệu</h1><p>Nhập batch số điện thoại có kiểm soát; số mới mặc định noindex.</p></div></header><article className="adminPanel"><ImportForm/></article><article className="adminPanel"><h2>Lịch sử batch</h2>{d?.data?.map((x:any)=><div className="adminRecord" key={x.id}><div><b>{x.source}</b><p>{x.accepted}/{x.total} nhận · {x.rejected} từ chối</p></div><span className="adminBadge">{x.status}</span></div>)}{!d?.data?.length&&<div className="emptyAdmin">Chưa có batch import.</div>}</article></section></main>}