import {redirect} from "next/navigation";import {isAdmin} from "../../../lib/admin-session";import AdminNav from "../_components/nav";import {adminAPI} from "../_lib/api";import Moderate from "../_components/moderate";
export default async function Page(){if(!(await isAdmin()))redirect("/admin/login");const data=await adminAPI("/v1/admin/reports?limit=100");return <main className="adminShell"><AdminNav/><section className="adminMain"><header className="adminTop"><div><h1>Reports & Reputation</h1><p>Danh sách báo cáo người dùng gửi về hệ thống.</p></div></header><article className="adminPanel">{data?.data?.map((x:any)=><div className="adminRecord" key={x.id}><div><b>{x.e164}</b><p>{x.reason}{x.comment?" · "+x.comment:""}</p></div>{x.status==="pending"?<Moderate kind="reports" id={x.id}/>:<span className="adminBadge">{x.status}</span>}</div>)}{!data?.data?.length&&<div className="emptyAdmin">Không có report.</div>}</article></section></main>}
