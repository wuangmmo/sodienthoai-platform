import {redirect} from "next/navigation";import {isAdmin} from "../../../lib/admin-session";import AdminNav from "../_components/nav";import {adminAPI} from "../_lib/api";export default async function Page(){if(!(await isAdmin()))redirect("/admin/login");const d=await adminAPI("/v1/admin/footprint?limit=100");return <main className="adminShell"><AdminNav/><section className="adminMain"><header className="adminTop"><div><h1>Internet Footprint</h1><p>Nguồn công khai được phát hiện và trạng thái xử lý.</p></div></header><article className="adminPanel">{d?.data?.map((x:any)=><div className="adminRecord" key={x.id}><div><b>{x.e164} · {x.domain}</b><p>{x.page_title||x.url}</p></div><span className="adminBadge">{x.category}</span></div>)}{!d?.data?.length&&<div className="emptyAdmin">Chưa có footprint.</div>}</article></section></main>}