import {redirect} from "next/navigation";import {isAdmin} from "../../../lib/admin-session";import AdminNav from "../_components/nav";import {adminAPI} from "../_lib/api";
export const metadata={title:"Số điện thoại · Admin",robots:{index:false,follow:false}};
export default async function Page({searchParams}:{searchParams:Promise<{q?:string}>}){if(!(await isAdmin()))redirect("/admin/login");const q=(await searchParams).q||"";const data=await adminAPI("/v1/admin/phones?limit=100&q="+encodeURIComponent(q));return <main className="adminShell"><AdminNav/><section className="adminMain"><header className="adminTop"><div><h1>Số điện thoại</h1><p>Tra cứu và kiểm tra dữ liệu số trong hệ thống.</p></div></header><form className="adminSearch"><input name="q" defaultValue={q} placeholder="Nhập số điện thoại..."/><button>Tìm kiếm</button></form><article className="adminPanel"><div className="adminTable"><div className="adminTR adminTH"><span>Số</span><span>Xác minh</span><span>SEO</span><span>Reports</span><span>Lượt tìm</span></div>{data?.data?.map((x:any)=><div className="adminTR" key={x.id}><b>{x.e164}</b><span>{x.verification_status}</span><span>{x.seo_status}</span><span>{x.report_count}</span><span>{x.search_count}</span></div>)}</div>{!data?.data?.length&&<div className="emptyAdmin">Chưa có dữ liệu phù hợp.</div>}</article></section></main>}
