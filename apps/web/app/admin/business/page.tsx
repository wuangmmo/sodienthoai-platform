import {redirect} from "next/navigation";import {isAdmin} from "../../../lib/admin-session";import AdminNav from "../_components/nav";import {adminAPI} from "../_lib/api";import Moderate from "../_components/moderate";
export default async function Page(){if(!(await isAdmin()))redirect("/admin/login");const [v,r,a]=await Promise.all([adminAPI("/v1/admin/business-verifications?limit=100"),adminAPI("/v1/admin/business-reviews?limit=100"),adminAPI("/v1/admin/appeals?limit=100")]);return <main className="adminShell"><AdminNav/><section className="adminMain"><header className="adminTop"><div><h1>Business & Appeals</h1><p>Xác minh doanh nghiệp, đánh giá và khiếu nại số điện thoại.</p></div></header><article className="adminPanel"><h2>Xác minh doanh nghiệp</h2>{v?.data?.map((x:any)=><div className="adminRecord" key={x.id}><div><b>{x.business_name||x.business_id}</b><p>{x.method} · {x.statement||"Không có ghi chú"}</p></div>{x.status==="pending"?<Moderate kind="business-verifications" id={x.id}/>:<span className="adminBadge">{x.status}</span>}</div>)}{!v?.data?.length&&<div className="emptyAdmin">Không có yêu cầu xác minh.</div>}</article><article className="adminPanel" style={{marginTop:14}}><h2>Đánh giá doanh nghiệp</h2>{r?.data?.map((x:any)=><div className="adminRecord" key={x.id}><div><b>{x.business_name||x.business_id} · {x.rating}/5</b><p>{x.body||"Không có nội dung"}</p></div>{x.status==="pending"?<Moderate kind="business-reviews" id={x.id}/>:<span className="adminBadge">{x.status}</span>}</div>)}{!r?.data?.length&&<div className="emptyAdmin">Không có review.</div>}</article><article className="adminPanel" style={{marginTop:14}}><h2>Khiếu nại số điện thoại</h2>{a?.data?.map((x:any)=><div className="adminRecord" key={x.id}><div><b>{x.e164}</b><p>{x.reason} · {x.statement}</p></div>{x.status==="pending"?<Moderate kind="appeals" id={x.id}/>:<span className="adminBadge">{x.status}</span>}</div>)}{!a?.data?.length&&<div className="emptyAdmin">Không có appeal.</div>}</article></section></main>}
