export function csrf(){return document.cookie.split("; ").find(x=>x.startsWith("control_csrf="))?.split("=")[1]||""}export function controlFetch(url:string,init:RequestInit={}){const method=(init.method||"GET").toUpperCase();const headers=new Headers(init.headers);if(!["GET","HEAD","OPTIONS"].includes(method))headers.set("x-csrf-token",decodeURIComponent(csrf()));return fetch(url,{...init,headers})}