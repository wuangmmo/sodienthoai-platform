"use client";import ControlShell from "../../components/control-shell";import {State,useControl} from "../../components/data";type Row={ID:number;Action:string;ResourceType:string;ResourceID:string;Actor:string;CreatedAt:string};export default function Audit(){const {data,error}=useControl<{audit:Row[]}>("audit");return <ControlShell><div className="top"><div><div className="title">Audit Logs</div><div className="sub">Lịch sử thay đổi quản trị quan trọng.</div></div></div>{!data?<State error={error}/>:<div className="tableWrap"><table><thead><tr><th>Thời gian</th><th>Actor</th><th>Action</th><th>Resource</th></tr></thead><tbody>{data.audit.map(x=><tr key={x.ID}><td>{new Date(x.CreatedAt).toLocaleString("vi-VN")}</td><td>{x.Actor}</td><td><code>{x.Action}</code></td><td>{x.ResourceType}<div className="muted">{x.ResourceID}</div></td></tr>)}</tbody></table></div>}</ControlShell>}