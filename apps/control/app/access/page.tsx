"use client";import ControlShell from "../../components/control-shell";import {State,useControl} from "../../components/data";import {controlFetch} from "../../lib/client";
type A={ID:string;AdminName:string;AdminEmail:string;RoleName:string;RoleSlug:string;SiteName:string;SiteID:string};
export default function Access(){const {data,error}=useControl<{assignments:A[]}>("scopes");async function revoke(x:A){if(!confirm("Thu hồi "+x.RoleName+" của "+x.AdminEmail+"?"))return;const r=await controlFetch("/api/control/scopes/"+x.ID,{method:"DELETE"});if(r.ok)location.reload();else{const j=await r.json().catch(()=>({}));alert(j.error||"Không thể thu hồi quyền")}}return <ControlShell><div className="top"><div><div className="title">Admin Access</div><div className="sub">Role assignment theo Organization / Site. Backend bảo vệ last Super Admin.</div></div></div>{!data?<State error={error}/>:<div className="tableWrap"><table><thead><tr><th>Administrator</th><th>Role</th><th>Scope</th><th></th></tr></thead><tbody>{data.assignments.map(x=><tr key={x.ID}><td><b>{x.AdminName}</b><div className="muted">{x.AdminEmail}</div></td><td>{x.RoleName}</td><td>{x.SiteID?x.SiteName:"Organization"}</td><td>{x.RoleSlug==="super-admin"?<span className="pill">protected</span>:<button className="danger" onClick={()=>revoke(x)}>Thu hồi</button>}</td></tr>)}</tbody></table></div>}</ControlShell>}