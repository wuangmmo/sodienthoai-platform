"use client";import ControlShell from "../components/control-shell";import {State,useControl} from "../components/data";
type Dashboard={phones:number;reports:number;claims:number;sites:number};
export default function Page(){const {data,error}=useControl<Dashboard>("dashboard");return <ControlShell><div className="top"><div><div className="title">Tổng quan hệ thống</div><div className="sub">Dữ liệu trực tiếp từ Go API và PostgreSQL</div></div><div className="badge">Sodienthoai Ecosystem</div></div>{!data?<State error={error}/>:<><div className="grid"><K n={data.phones} l="Số điện thoại"/><K n={data.reports} l="Báo cáo"/><K n={data.claims} l="Claims"/><K n={data.sites} l="Sites / Platforms"/></div><section className="section"><div className="card"><b>V44.2 Control Center</b><p className="muted">Các KPI trên được truy vấn từ hệ thống thật. Quyền truy cập được kiểm tra tại Go API trước khi dữ liệu được trả về.</p></div></section></>}</ControlShell>}function K({n,l}:{n:number;l:string}){return <div className="card"><span className="muted">{l}</span><div className="kpi">{n.toLocaleString("vi-VN")}</div></div>}