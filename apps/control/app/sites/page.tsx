"use client";import ControlShell from "../../components/control-shell";import {State,useControl} from "../../components/data";type Site={ID:string;Name:string;Domain:string;SiteType:string;ManagementMode:string;Status:string};export default function Sites(){const {data,error}=useControl<{sites:Site[]}>("sites");return <ControlShell><div className="top"><div><div className="title">Sites / Projects</div><div className="sub">Managed platforms và connected platforms theo scope tài khoản.</div></div></div>{!data?<State error={error}/>:<div className="tableWrap"><table><thead><tr><th>Website</th><th>Loại</th><th>Quản trị</th><th>Trạng thái</th></tr></thead><tbody>{data.sites.map(s=><tr key={s.ID}><td><b>{s.Name}</b><div className="muted">{s.Domain}</div></td><td>{s.SiteType}</td><td><span className="pill">{s.ManagementMode}</span></td><td><span className={"pill "+(s.Status==="active"?"green":"")}>{s.Status}</span></td></tr>)}</tbody></table></div>}</ControlShell>}